{
  "name": "tumiki-proxy",
  "version": "0.1.5",
  "description": "Transparent MCP logging proxy - Phase 1: stdio-only / Phase 1.5: Multi-transport (stdio, HTTP/StreamableHTTP, SSE) with SDK",
  "type": "module",
  "bin": {
    "tumiki-proxy": "./dist/index.js",
    "tumiki-proxy-sdk": "./dist/index-sdk.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "keywords": [
    "mcp",
    "proxy",
    "logging",
    "json-rpc"
  ],
  "author": "tumiki-proxy contributors",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2"
  }
}
